

 
       
 <div class="col-sm-offset-3 col-md-6 col-md-offset-3 main">

<div class="sub-titles">New Asset</div>
 <table class="table table-striped table-fluid">

     <div class="form-group">
    <form class="form-horizontal" action="index.php" method="post" id="new_asset_edit_form">
        <input type="hidden" name="action" value="update_new_asset" />

     <input type="hidden" name="ass_id" value="{{ ass_id }}" />
          <input type="hidden" name="cid" value="{{ cate_id }}" />

     <tr>
      <td class="asset_code">&nbsp;&nbsp;{{ ass_id }} </td>

         <input type="hidden" name="aid" value="{{ ass_id }}" /> </tr>

         <tr><td><label>	Select New Category</label>
                <select name="cid" class="form-control">

          {% for category in categories %}
                  <option value="{{ category.category_id }}"> {{ category.category_name }} </option>
          {% endfor  %}
</td></tr>
                </select>
        <tr>   <td>  <input type="submit" value="Submit" class="btn btn-primary pull-right"/></td>

</tr>
    </form>

 </table>

</div>
</div>
</body>
</html>
